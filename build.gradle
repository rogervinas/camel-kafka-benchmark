buildscript {
    ext {
        camelVersion = '2.25.4'
    }
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
}

plugins {
    id 'idea'
    id 'java'
    id 'org.springframework.boot' version '3.4.3'
}

group 'com.rogervinas'

apply plugin: 'java'

repositories {
    mavenCentral()
    maven { url 'https://repo.spring.io/milestone' }
    maven { url 'https://repo.eclipse.org/content/groups/releases' }
}

dependencies {
    // Spring Shell
    implementation("org.springframework.shell:spring-shell-starter:3.4.0")

    // Apache Camel
    implementation platform("org.apache.camel:camel-spring-boot-dependencies:$camelVersion")
    implementation("org.apache.camel:camel-kafka")
    implementation("org.apache.camel:camel-test")

    implementation("org.apache.commons:commons-math3:3.6.1")
    implementation("org.apache.commons:commons-io:1.3.2")
    implementation("org.apache.commons:commons-lang3:3.17.0")

    // Test
    testImplementation("org.junit.jupiter:junit-jupiter-api:5.12.0")
    testImplementation("org.junit.jupiter:junit-jupiter-params:5.12.0")
    testRuntime("org.junit.jupiter:junit-jupiter-engine:5.12.0")
    testImplementation("junit:junit:4.13.2")
    testRuntime("org.junit.vintage:junit-vintage-engine:5.12.0")
    testImplementation("org.assertj:assertj-core:3.27.3")
}

test {
    useJUnitPlatform()
}
